# Removes the committed development Secret from the prod build. Production
# credentials never live in this repository; the cluster is expected to already
# hold `resumeforge-secrets`.
$patch: delete
apiVersion: v1
kind: Secret
metadata:
  name: resumeforge-secrets
