# Provisioned, not clicked. A Grafana whose datasource exists only in its volume
# is a Grafana nobody else can reproduce.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: prometheus
    isDefault: true
    editable: false
