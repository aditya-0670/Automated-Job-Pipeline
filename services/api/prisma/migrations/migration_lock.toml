# Generated by hand: `migrate diff --from-empty` writes the SQL but not this
# file, which only `migrate dev` creates -- and `migrate dev` is the command this
# project never runs (see schema.prisma).
provider = "postgresql"
