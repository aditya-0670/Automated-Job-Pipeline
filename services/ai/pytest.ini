[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --tb=short
markers =
    live: spends real Gemini quota; requires RUN_LIVE_LLM_TESTS=1
